# 开发守则

本项目是局域网文件传输工具，安全是最高优先级。以下规则适用于所有开发工作（包括 AI 协作者）。

## 信息安全硬性要求

- 任何 API Key、密钥、令牌或口令**绝不**以明文形式写入代码、配置文件、文档或提交历史。
- 密钥必须由程序运行时生成（如 `random_secret()`）或由用户通过受信渠道提供；禁止硬编码默认密钥。
- 共享口令只能以加盐哈希形式存储和比对（当前实现使用 `scrypt`，见 `src-tauri/src/main.rs` 的 `make_passcode_record`）。
- 链接令牌必须加密且不含真实路径（当前实现使用 AES-256-GCM）。
- 禁止把真实文件路径、口令哈希或任何敏感内部状态暴露给访客 API。
- 环境变量文件（`.env*`）已被 `.gitignore` 忽略；新增示例文件不得包含真实密钥占位值以外的内容。
- 文件路径访问必须经过边界校验（`safe_resolve`），访客不能跳出共享根目录。

## 代码与提交要求

- 每次改动后必须通过 `npm run lint`；改动 Rust 代码时运行 `cargo check`。
- 提交前检查 diff，确认没有引入密钥、明文口令或敏感数据。
- 若发现历史提交或文件中存在已泄露的密钥，立即向用户报告并协助轮换，不能仅删除文件了事。
- 新增依赖需说明用途；不再使用的依赖和文件应及时清理。
